services:
  - type: web
    name: skill-swap
    env: static
    branch: main
    buildCommand: npm install && npm run build
    publishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html